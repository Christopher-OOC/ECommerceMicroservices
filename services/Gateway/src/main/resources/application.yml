spring:
  application:
    name: Gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes[0]:
        id: customer-service
        uri: lb://Customer
        predicates[0]: Path=/api/v1/customers/**
      routes[1]:
        id: customer-service
        uri: lb://Customer
        predicates[0]: Path=/api/v1/customers/**
      routes[2]:
        id: customer-service
        uri: lb://Customer
        predicates[0]: Path=/api/v1/customers/**
